#include <stdio.h>

void main() {

	int a; // dichiaro una zona di memoria che identifico come 'a'
	
	// assegnamento, ovvero valuto l'espressione di dx e
	// la copio nella zona di memoria che ho identificato come 'a'
	a = 5;

	float f1;
	f1 = 5.6; // => b * 2 ^ ((-s)e) => [s][b22..b0][e7...e0]

	float f2 = 4.2;

	float sumf1f2;
	sumf1f2 = f1 + f2; 

	printf("stampa a:%d poi stampa b:%d \n", a, 10);
	printf("sumf1f2 %f \n", sumf1f2);

}